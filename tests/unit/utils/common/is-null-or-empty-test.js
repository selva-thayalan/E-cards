import commonIsNullOrEmpty from 'e-cards/utils/common/is-null-or-empty';
import { module, test } from 'qunit';

module('Unit | Utility | common/is-null-or-empty', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = commonIsNullOrEmpty();
    assert.ok(result);
  });
});
